import styled from 'styled-components';

export { Container, Wrapper, Content, Title } from '../styles';

export const Main = styled.div`
  margin-left: 10px;
  display: flex;

  h3 {
    padding-top: 0.25em;
    font-weight: 300;
  }
`;
