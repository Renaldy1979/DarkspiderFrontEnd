import React from 'react';

import { Container } from './styles';

const SideBar: React.FC = () => {
  return (
    <Container>
      <h1>sidebar</h1>
    </Container>
  );
};

export default SideBar;
