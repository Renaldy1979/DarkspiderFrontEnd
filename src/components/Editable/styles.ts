import styled from 'styled-components';

export const Container = styled.div`
  input,
  textarea,
  span {
    font-size: 1rem;
    font-weight: normal;
    text-align: center;
    width: 100%;
    color: var(--text-body-light);
  }

  textarea {
    text-align: left;
  }
`;
